import mascotLogo from "@/assets/images/moscot-logo.png";
import mascotWelcome from "@/assets/images/mascot-welcome.png";

/**
 * Centralized image imports.
 * Add every app image here and use it through the `images` object,
 * e.g. `<Image source={images.mascotLogo} />`.
 */
export const images = {
  mascotLogo,
  mascotWelcome,
} as const;
